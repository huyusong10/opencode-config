# OpenCode 特定要求

- OpenCode 全局指令目标文件为 `~/.config/opencode/AGENTS.md`。
- OpenCode agents、commands、plugins 分别安装到 `agent/`、`command/`、`plugin/`。
- OpenCode 功能拓展一定要参考官方文档：`https://opencode.ai/docs/zh-cn/sdk/`。
- 若需要 SDK，使用以下命令安装：`npm install @opencode-ai/sdk`。
