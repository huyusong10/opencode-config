## 用户偏好

- 不要使用 **Unicode 连字符** `‑` (U+2011)；请使用 **ASCII 连字符** `-` (U+002D)
- 始终使用 **中文** 回复，编写文档
- 代码注释和文档使用 **英文** 编写
- Python 任务使用 `uv`；如未安装，则回退到 `python` 和 `pip`
- 对于 **新项目**（新创建、看似空白的）：使用 **4个空格** 缩进
- 对于 **现有项目**：先检测现有风格，方法如下：
  - 风格配置文件（`.editorconfig`、`pyproject.toml`、`.stylua.json`、`.clang-format` 等）
  - 现有代码缩进模式
  - 然后遵循检测到的风格

---

## 操作规范

### 调试流程

遇到 bug 或测试失败时：

```
1. 加载 skill: systematic-debugging
2. 阅读错误信息，理解问题
3. 复现问题，确认存在
4. 追踪数据流，定位根因
5. 形成假设，最小化验证
6. 先写失败测试，再修复
7. 验证修复，确保测试通过
```

**铁律：没有根因调查，没有修复。**

### Codeact 模式优先

对于 **复杂流程**，优先编写并执行代码脚本，而非直接调用工具。

**适用场景**：
- 需要多步骤数据处理或转换
- 需要循环、条件分支等复杂逻辑
- 需要状态跟踪或错误恢复
- 操作之间存在依赖关系

**对比示例**：

❌ 直接工具调用（低效、易出错）：
```
用户: 分析 src/ 目录下所有 TypeScript 文件，找出未使用的导出

错误方式:
1. 使用 glob 找到所有 .ts 文件
2. 逐个 read 读取文件内容
3. 在上下文中手动分析每个文件的 import/export
4. 记忆并对比所有依赖关系
5. 手动汇总结果
```

✅ Codeact 模式（高效、可验证）：
```python
# scripts/find_unused_exports.py
import subprocess
import json
from pathlib import Path

# 使用 ts-morph 或正则分析所有导出
exports = {}  # { file: { export_name: line } }
imports = set()  # 所有被导入的名称

# 分析完成后输出 JSON 报告
print(json.dumps({"unused": unused_exports}, indent=2))
```

然后执行脚本获取结果，代码可复用、可调试、可验证。

### Subagent 使用原则

**何时使用 Subagent**：

| 场景 | 判断标准 | 示例 |
|------|----------|------|
| 独立子任务 | 任务边界清晰，可独立完成 | `@spec-feasible` 研究技术方案可行性 |
| 需要隔离上下文 | 任务需要大量探索，避免污染主上下文 | `@task-executor` 批量执行独立任务 |
| 专门职责 | 需要特定领域知识或角色 | `@committer` 处理 git 提交 |
| 只读审查 | 需要公正的第三方视角 | `@spec-review` 审查实现是否符合规范 |

**何时直接处理**：

| 场景 | 判断标准 | 示例 |
|------|----------|------|
| 简单操作 | 1-3 步可完成 | 读取单个文件、编辑配置 |
| 需要即时反馈 | 用户需要看到实时过程 | 交互式调试、逐步解释 |
| 上下文依赖 | 任务需要当前对话的全部上下文 | 复杂的跨文件重构决策 |

**决策流程**：

```
任务是否需要 > 5 个工具调用？
    ↓ 是
任务是否边界清晰可独立完成？
    ↓ 是
是否需要隔离上下文或专门职责？
    ↓ 是
→ 使用 Subagent
```
