# 顶层规则

## 判断框架

AGENTS 不是流程清单，而是协作判断契约。每次行动都围绕同一条链路判断：

`稳定声明 -> 证据/边界 -> 风险档位 -> 执行动作 -> 验证/清理`

- 稳定声明：系统应长期保持的行为、契约、设计边界或用户可观察结果。
- 证据/边界：能证明声明仍成立的 design、tests、schema、类型、lint、artifact、人工记录或运行结果。
- 风险档位：这次改动是否影响公开契约、数据、安全、权限、多语言、可访问性、核心旅程、迁移成本或回滚难度。
- 执行动作：直接实现、先澄清、先整理 design、先补验证、先征询同意，或先缩小范围。
- 验证/清理：运行相关验证，并清理同边界内低价值、重复或过期资产。

低风险、可逆、只影响内部实现的小改动可以用这套链路做简化判断后直接执行；不要把所有小修都扩大成文档和测试工程。风险升高、契约变化或证据不足时，才显式放慢并补齐设计与验证。

## 基本规则

1. 不把 prompt 当作事实来源；当用户需求与事实、代码、design、tests 或契约冲突时，先指出不确定点，给出更稳建议，并征询同意。
2. 不只修表面问题；每个改进点都要抽象背后的稳定问题、约束和同类风险，用最小但完整的系统性方案处理。
3. 修改前先定位稳定边界；有 design 入口就从 design 进入，没有 design 体系时从 README、公开接口、tests 或邻近代码推断当前边界。
4. 行为变化必须有证据；影响对外接口、用户主流程、稳定数据结构、权限边界、多语言、可访问性或安全语义时，同步检查 design 与 tests。
5. 文档和测试都是资产；不要把实现细节、临时状态、私有变量、DOM 层级、CSS 类名或具体文案写成稳定契约。
6. 历史坑优先沉淀为测试、类型、schema、lint 或当前 design；只有无法自动化且复发风险高时，才写 regression。
7. 资产预算优先于新增材料；当 design、tests、decision、regression 或 AGENTS 规则重复、过细或难以维护时，先合并、删除或重构。
8. 高风险选择必须等待同意；会破坏兼容性、改变公开契约、触碰用户数据、放大迁移成本或难以回滚的操作，不用假设替用户决定。
9. 完成后要验证，并检查同区域是否有低风险、同边界内的顺手治理机会。

# 需求澄清与同意

## 本章作用

需求澄清用于保护目标不被不稳定输入污染。用户可能记错现状、遗漏约束，或提出会伤害长期可维护性的实现方式；Agent 应保护用户真正要达成的稳定目标，而不是机械执行字面方案。

## 必须确认的情况

- 目标、范围或验收标准不清，继续做会主要依赖猜测。
- 用户描述与代码、design、tests、线上约束或已知事实冲突。
- 用户要求可能破坏公开契约、数据安全、权限边界、隐私、可访问性、多语言能力或核心用户旅程。
- 用户指定的实现方式明显不适合当前结构，或存在更低风险且更符合目标的做法。
- 用户要求跳过必要的 design、tests、审查、迁移或验证步骤。
- 用户要求执行高成本、破坏性、难回滚或影响面很大的操作。

## 澄清方式

- 先说明不确定点或风险，再给出推荐方案。
- 推荐方案应说明它保护了什么稳定目标，例如契约兼容性、数据安全、验证成本或未来维护性。
- 明确询问用户是否同意推荐方案，或是否仍要按原方案执行。
- 低风险、可逆的小选择可以说明假设后继续；会影响契约或用户数据的选择必须等待明确同意。

# 问题抽象与系统性改进

## 本章作用

具体请求通常只是某个稳定问题的一个样本。好的改动应消除同类风险，而不是只让眼前用例通过；但系统性不等于自动扩大范围，任何扩大契约、迁移成本或风险面的方案都要先征询同意。

## 判断步骤

1. 识别用户真正想消除的痛点、风险、约束或不变量。
2. 查找同类入口、组件、数据结构、状态流、权限边界或测试锚点是否存在相同风险。
3. 优先选择覆盖同类问题的最小稳定改法，而不是单点补丁。
4. 如果抽象出新的稳定声明或契约，同步检查是否需要更新 design、schema、类型或契约测试。
5. 若更稳方案会扩大范围、改变公开行为或增加迁移成本，先说明取舍并等待同意。

# 工作入口

## 本章作用

修改入口不是代码文件，而是稳定边界。先知道模块为什么存在、承诺什么、依赖谁、如何验证，再进入实现；否则容易把局部实现误认为系统契约。

## 入口规则

- 项目维护 `design/README.md` 时，修改前先从该索引找到相关设计边界、验证入口和历史记录。
- 没有 design 体系时，先从 README、公开接口、tests、schema、类型或邻近代码推断当前稳定边界，不为了形式先造一套完整 design。
- 若 design 缺失、过期、重叠或无法指导本次变更，只整理到足以指导本次实施与验证的状态。
- 若改动影响对外接口、页面路由、核心组件 Props、稳定数据结构或用户主流程，必须在同一次任务中同步更新 design 与 tests。
- 若只是内部重构且对外契约不变，不应为了迁就实现细节改 design 或契约测试；若原测试因此失败，优先视为契约被破坏。
- 完成特性后，主动检查同区域是否存在可顺手治理的技术债，例如重复逻辑、脆弱分支、难测模块、过期文档或低价值测试。

# Design 原则

## 本章作用

Design 是稳定边界地图，不是实现流水账。它回答“这个模块承诺什么、依赖什么、用什么证据证明仍成立”，让未来改动能先定位契约，再判断实现是否偏离。

## 应写什么

- 模块职责：这个模块存在的唯一理由。
- 对外契约：公开接口、事件、数据结构、错误语义。
- 数据流转：输入 -> 处理边界 -> 输出。
- 依赖边界：依赖谁、期望对方提供什么稳定行为。
- 验证入口：哪些 tests、schema、类型或 lint 能证明契约仍成立。
- 失效含义：当验证失败或 design 与实现冲突时，应被理解为契约变化、行为退化、文档过期还是证据不足。

## 不要写什么

- 不记录内部函数、临时变量、缓存 key、CSS 类名、DOM 层级、框架技巧或实现步骤。
- 不为了显得完整，把显而易见的实现细节抄进 design。
- 不把具体中文或英文文案写成契约本身。
- 不出现每个组件一个文档、每个小改动一页说明这类碎片化产物。

## 组织方式

- 一份 design 只覆盖一个稳定边界；边界重叠时合并，边界过大时拆分。
- 优先用表格、列表、流程图描述稳定边界。
- design 要短而稳，宁可少写实现，也不要写错契约。

# Tests 原则

## 本章作用

测试用例是对稳定行为声明的证据设计，不是实现结构镜像。设计测试时先明确它要证明什么行为、使用什么证据、是否能由机器稳定裁决、适合哪个运行档位，以及失败后应如何解释。只有当契约变化、证据取舍或运行成本会影响用户决策时，才需要把这些判断显式讲给用户。

## 证据设计步骤

1. 从 design、schema、类型或公开接口中找出要保护的稳定声明。
2. 判断该声明是否稳定、是否可机器裁决；不稳定判断不要写成硬断言。
3. 选择最小足够证据：用户可观察结果、公开返回值、结构化事件、持久化 artifact、可访问 UI、错误语义、真实外部边界、截图、文本索引或人工评审记录。
4. 决定运行档位：默认快检、触达模块的 focused 检查、UI/浏览器 journey、发布或外部依赖 probe、显式 opt-in 的 review/scenario/experiment。
5. 明确失败含义：契约破坏、行为退化、环境缺失、质量风险或探索发现。

## 常见验证形态

这些名称是常见形态，不是强制目录模板：

- contract/default-fast：稳定公开契约、数据语义、schema、CLI/API、静态资产，可机器稳定裁决，适合默认质量门。
- integration/focused：模块边界和数据流，跟随被触达模块运行。
- journey：少量核心用户旅程，需要页面、浏览器、客户端状态或本地服务证据。
- probe：真实 provider、真实浏览器、真实第三方服务或真实宿主环境，只验证最小外部边界契约。
- review：视觉质量、表达是否自然、复杂产品心智、模型质量等模糊判断，收集证据供人工或 Agent 评审，不伪装成硬断言。
- scenario：无法被自动 checks、probes 或 reviews 覆盖的连续人工判断路径，不能成为自动化缺口的堆放处。
- experiment：高成本、研究型或 provider 依赖强的真实任务；只有当其行为成为稳定发布边界时，才提升为 probe、journey 或 contract。

## 断言原则

- 默认质量门应快且确定，只包含能稳定裁决的契约检查和必要静态验证。
- 先断言用户可观察结果、公开返回值、公开事件、可达路径和错误语义。
- 只断言 design、schema、类型或公开接口承诺的字段；不要因为当前实现返回了某个字段就把它锁成契约。
- 对复杂对象优先断言语义不变量、必要字段集合和兼容性边界，而不是完整对象、字段顺序、默认值细节或中间态 shape。
- 不断言私有变量、内部调用顺序、临时状态。
- 不依赖 CSS 类名、DOM 层级、像素级布局或整页快照。
- 不把静态文案当作主要断言锚点。
- 前端测试优先使用可访问性角色、稳定属性或 `data-testid`。
- 若改动会让既有契约测试失效，必须明确告知用户这是契约变更，不得静默修改。

# 演进记忆

## 本章作用

历史记录不是新的真理来源，而是帮助当前系统避免重复犯错的证据。凡是能沉淀为当前 design、tests、schema、类型或 lint 的教训，都应回流到这些资产，而不是永久留在历史文件里。

## 记录边界

- `design/` 表达当前稳定设计边界与公开契约。
- `tests/` 验证当前行为和证据契约。
- `design/decisions/` 记录重要架构取舍，说明背景、方案、取舍、状态和关联验证。
- `design/regressions/` 只记录无法自动化且复发风险高的历史坑。
- decision 或 regression 失效时，标记 `Superseded`、写清替代关系，并在后续清理中删除或归档。

# 资产预算

## 本章作用

文档、测试和规则都会占用未来维护者的注意力。资产预算不是固定行数，而是信息密度约束：稳定边界越多、风险越高，才值得增加更多设计与验证资产。

## 预算规则

- 超预算信号包括：同一边界多份重复文档、测试只映射实现分支、文档没有验证入口、小改动需要读大量材料、AGENTS 规则反复解释同一件事。
- 超预算时先重构：合并重叠文档、删除过期测试、把历史教训转成自动验证、把实现细节移出契约。
- 新增 design、tests、decision 或 regression 前，先检查是否能更新既有稳定边界，而不是再开一份新文件。
- target 专属规则只有在存在真实、有效、不可共享的工具差异时才新增；安装路径、目录说明、空泛提醒不得伪装成规则占用上下文。

# 多语言与可访问性

## 本章作用

展示语言和具体文案会变化，稳定契约应保护语义、结构、权限和可达性。多语言和可访问性不是后续润色，而是用户能否可靠理解和操作系统的一部分。

## 约束

- 代码、design、tests、数据结构与对外契约都不要默认只有一种语言。
- 不把具体中文或英文文案当作稳定契约；文案可以变化，语义和结构才是更稳定的边界。
- 前端交互、可访问性名称、测试选择器和错误语义应避免依赖某一种展示语言。
- 新增用户可见内容时，应优先考虑后续接入国际化或替换文案的成本。
- 多语言、可访问性和公开契约同时变化时，优先保护语义一致性和可验证性。

# Codex 专属规则

## 浏览器调用

### 本章作用

浏览器路径是验证环境的一部分。选择内置浏览器、系统浏览器、Playwright 或桌面浏览器会影响登录态、可见性、可控性和复现方式；先选对环境，再执行导航、点击、截图或调试。

### 选择规则

- 用户要求打开、导航、点击、检查、截图或调试本地 Web 页面时，优先使用 Codex 内置浏览器能力，尤其是 `localhost`、`127.0.0.1`、`::1` 和 `file://` 目标。
- 只有在用户明确要求系统默认浏览器、需要真实个人浏览器状态，或任务必须依赖外部浏览器登录态、扩展程序、证书、协议处理器时，才打开系统外部浏览器。
- 不要把 macOS `open`、系统默认浏览器、Playwright 启动的 Chromium/Chrome、Computer Use 控制的桌面浏览器混作同一种浏览器路径。
- 使用 Playwright 或项目测试命令启动浏览器时，应把它视为自动化验证环境，而不是默认外部浏览器；除非用户同意，不用它替代内置浏览器完成人工式页面检查。
- 当浏览器路径会影响结果，或需要偏离内置浏览器优先策略时，先简短说明将使用哪一种浏览器以及原因。
