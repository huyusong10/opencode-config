# 顶层规则

1. 不要假定用户总是正确的；遇到不明确、不合适、与事实或契约冲突的需求，先确认、给建议、征询同意，再执行。
2. 用户提出每个改进点时，应抽象背后的稳定问题、约束与同类场景，给出最小但完整的系统性方案；扩大范围、改变契约或增加风险前必须先征询同意。
3. 修改代码前，先找到对应 design，理解模块职责、公开接口、数据流、依赖边界和错误语义；缺失契约时，只补本次任务需要的最小 design。
4. 任何影响外部行为的改动，都必须同步检查并更新 `design/` 与 `tests/`。
5. design、tests、decision、regression 和 AGENTS 规则都不要写得过细、过碎、过度绑定实现；超出项目规模预算时，先重构再新增。
6. 历史坑优先转成测试、类型、schema、lint 或当前 design；只有无法自动化且复发风险高时，才写 regression。
7. 不要把内部实现细节、具体文案、DOM 层级、CSS 类名、私有变量或临时状态写成稳定契约。
8. 多语言、可访问性、公开契约、权限边界、隐私、安全和核心用户旅程的变化必须特别审视；可能破坏兼容性时先确认。
9. 完成改动后，运行相关验证，并检查同区域是否有可低风险顺手治理的技术债。

# 需求澄清与同意

### 目标

- 不把 prompt 当作事实来源；用户可能记错、遗漏上下文，或提出会伤害长期可维护性的方案。
- 在需求不清、证据冲突或方案不合适时，尽早说明风险，给出更稳的建议，并等待用户同意后再执行。

### 必须先确认的情况

- 目标、范围、验收标准不明确，继续做会导致大量猜测。
- 用户描述与代码、design、tests、线上约束或已知事实冲突。
- 用户要求可能破坏公开契约、数据安全、权限边界、隐私、可访问性、多语言能力或核心用户旅程。
- 用户指定的实现方式明显不适合当前结构，或存在更低风险、更符合目标的做法。
- 用户要求跳过必要的 design、tests、审查、迁移或验证步骤。
- 用户要求执行高成本、破坏性、难回滚或影响面很大的操作。

### 如何确认

- 先简短说明不确定点或风险，不要直接否定用户。
- 给出推荐方案，并说明它为什么更适合当前目标。
- 明确询问用户是否同意推荐方案，或是否仍要按原方案执行。
- 低风险、可逆的小选择可以说明假设后继续；会影响契约或用户数据的选择必须等待明确同意。

# 问题抽象与系统性改进

### 目标

- 不只修复表面问题，而是识别背后的稳定不变量、系统约束和同类风险。
- 让每个改进点尽可能推动系统更一致、更可维护、更不容易在相邻场景再次出错。
- 系统性思考不等于自动扩大实现范围；扩大范围、改变契约、增加迁移成本或引入架构调整前，必须先说明建议并征询同意。

### 如何思考

- 先问：用户真正想消除的痛点、风险、约束或不变量是什么。
- 再看：当前问题是否只是一个实例；同类入口、组件、数据结构、状态流或测试锚点是否也有同样风险。
- 方案优先选择能覆盖同类问题的最小稳定改法，而不是只在单点打补丁。
- 若抽象出更稳定的规则或契约，同步检查是否需要更新 design 和契约测试。

# 工作入口

- 修改前先从 `design/README.md` 找到相关契约、验证入口和历史记录；不要先钻内部实现。
- 若改动影响以下任一项，必须在同一次任务中同步更新 design 与 tests：
  - 对外接口
  - 页面路由
  - 核心组件 Props
  - 稳定数据结构
  - 用户主流程
- 若只是内部重构且对外契约不变：
  - 不应为了配合实现去改 design
  - 不应为了配合实现去改契约测试
  - 若原测试因此失败，优先视为契约被破坏
- 完成特性后，主动检查同区域是否存在可顺手治理的技术债，例如重复逻辑、脆弱分支、难测模块、过期文档或低价值测试。

# Design 原则

### 应写什么

- 模块职责：这个模块存在的唯一理由。
- 对外契约：公开接口、事件、数据结构、错误语义。
- 数据流转：输入 -> 处理边界 -> 输出。
- 依赖边界：依赖谁、期望对方提供什么稳定行为。
- 验证入口：哪些 tests/schema/lint 能证明契约仍成立。

### 不要写什么

- 不要记录内部函数、临时变量、缓存 key、CSS 类名、DOM 层级、框架技巧或实现步骤。
- 不要为了显得完整，把显而易见的实现细节抄进 design。
- 不要把具体中文或英文文案写成契约本身。
- 不要出现每个组件一个文档、每个小改动一页说明这类碎片化产物。

### 组织方式

- 一份 design 只覆盖一个稳定边界；边界重叠时合并，边界过大时拆分。
- 优先用表格、列表、流程图描述稳定边界。
- design 要短而稳，宁可少写实现，也不要写错契约。

# Tests 原则

### 目标

- tests 是重构安全网，首先锁定行为与契约，而不是锁定实现。
- 测试失败必须有意义：要么契约变了，要么行为退化了。

### 分层优先级

- 少量高价值 E2E 锁定核心用户旅程。
- 接口/集成测试锁定模块边界与数据流。
- 单元测试只用于复杂且稳定的规则、算法或边界条件。

### 断言原则

- 先断言用户可观察结果、公开返回值、公开事件、可达路径和错误语义。
- 只断言 design、schema、类型或公开接口承诺的字段；不要因为当前实现返回了某个字段就把它锁成契约。
- 对复杂对象优先断言语义不变量、必要字段集合和兼容性边界，而不是完整对象、字段顺序、默认值细节或中间态 shape。
- 不要断言私有变量、内部调用顺序、临时状态。
- 不要依赖 CSS 类名、DOM 层级、像素级布局或整页快照。
- 不要把静态文案当作主要断言锚点。
- 前端测试优先使用可访问性角色、稳定属性或 `data-testid`。
- 若改动会让既有契约测试失效，必须明确告知用户这是契约变更，不得静默修改。

# 演进记忆

- `design/` 表达当前稳定契约，`tests/` 验证当前行为，`decisions/` 与 `regressions/` 补足演进视角。
- 重要架构取舍写入 `design/decisions/`，说明背景、方案、取舍、状态和关联验证。
- 复发型历史坑写入 `design/regressions/`，但只有无法被测试、schema、lint 或当前 design 表达时才新增。
- 历史记录不是新的真理来源；能沉淀为当前契约或自动验证的内容，应回流到 design、tests、schema 或 lint。
- decision 或 regression 失效时，标记 `Superseded`、写清替代关系，并在后续清理中删除或归档。

# 资产预算

- 预算不是固定行数，而是相对项目规模；稳定边界、公开契约、核心流程、高风险历史坑越多，允许的文档和测试才相应增长。
- 超预算信号包括：同一边界多份重复文档、测试只映射实现分支、文档没有验证入口、小改动需要读大量材料、AGENTS 规则反复解释同一件事。
- 超预算时先重构：合并重叠文档、删除过期测试、把历史教训转成自动验证、把实现细节移出契约。
- 新增 design、tests、decision 或 regression 前，先检查是否能更新既有稳定边界，而不是再开一份新文件。
- target 专属规则只有在存在真实、有效、不可共享的工具差异时才新增；安装路径、目录说明、空泛提醒不得伪装成规则占用上下文。

# 多语言与可访问性

- 代码、design、tests、数据结构与对外契约都不要默认只有一种语言。
- 不要把具体中文或英文文案当作稳定契约；文案可以变化，语义和结构才是更稳定的边界。
- 前端交互、可访问性名称、测试选择器和错误语义应避免依赖某一种展示语言。
- 新增用户可见内容时，应优先考虑后续接入国际化或替换文案的成本。
- 多语言、可访问性和公开契约同时变化时，优先保护语义一致性和可验证性。
